// p06_tail_recursion: tail recursion elimination
int sumTo(int n, int acc) {
    if (n == 0) {
        return acc;
    }
    return sumTo(n - 1, acc + n);
}

int main() {
    return sumTo(100, 0);   // 5050
}
